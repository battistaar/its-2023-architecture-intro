# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build api-server-node`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t api-server-node`.
FROM docker.io/node:lts-alpine
WORKDIR /app

RUN addgroup --system api-server-node && \
          adduser --system -G api-server-node api-server-node

COPY dist/apps/api-node api-server-node
RUN chown -R api-server-node:api-server-node .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix api-server-node --omit=dev -f install

CMD [ "node", "api-server-node" ]
